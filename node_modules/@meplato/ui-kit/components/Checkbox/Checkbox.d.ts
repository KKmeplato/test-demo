import { CheckboxProps } from "./types";
export declare const Checkbox: {
    ({ id, checked, className, disabled, error, hint, label, onChange, ...rest }: CheckboxProps): import("react/jsx-runtime").JSX.Element;
    displayName: string;
};
